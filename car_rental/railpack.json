{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "python /app/car_rental/manage.py migrate && gunicorn --chdir /app/car_rental car_rental.wsgi:application --bind 0.0.0.0:$PORT"
  }
}